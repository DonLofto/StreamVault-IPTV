// Compose compiler stability configuration for StreamVault-IPTV
// Classes matching these patterns are considered stable by the Compose compiler,
// enabling smart recomposition skipping for lazy grids, lists, and row cards.

com.streamvault.domain.model.*
kotlinx.collections.immutable.*
java.time.*
java.time.temporal.*
